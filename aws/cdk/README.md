# AWS CDK

This directory contains the AWS CDK app for Francis Family Cloud.

Current purpose:

- create or manage the S3 bucket used by the app
- optionally create DynamoDB tables for metadata or auxiliary state

You can deploy this path when you want reproducible infrastructure instead of manual CLI setup.

